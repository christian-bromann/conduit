{
  "name": "conduit-example",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx @langchain/langgraph-cli dev --port 2024 --no-browser",
    "deploy": "bun ./scripts/deploy.ts",
    "deploy:no-wait": "bun ./scripts/deploy.ts --no-wait"
  },
  "dependencies": {
    "@conduit/core": "workspace:*",
    "@conduit/discord": "workspace:*",
    "@conduit/slack": "workspace:*",
    "@conduit/whatsapp": "workspace:*",
    "@langchain/anthropic": "^1.3.25",
    "@langchain/langgraph": "^1.2.5",
    "langchain": "^1.2.32"
  }
}
